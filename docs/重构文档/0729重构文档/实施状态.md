# Coomi Rust 0729 实施状态

更新时间：2026-07-30

## 已完成

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 工作分支 | `coomi-rs-0729` | 当前 Git branch |
| 固定 Codex 基线 | `9a6668f674d74b35418fa534b3b6285a315d0765` | `UPSTREAM_CODEX.toml` |
| 上游导入校验 | 5,394 个文件，逐路径 SHA-256 差异 0 | 导入校验记录 |
| 纯上游提交 | `3e788b384c2c796c5288672d5c49a04fa47de45c` | Git history |
| Rust 工具链 | Rust/Cargo 1.95.0，rustfmt、Clippy、rust-src | `rust-toolchain.toml` |
| Windows 编译器 | VS 2022 MSVC x64 | 本机构建验证 |
| Coomi CLI 骨架 | TUI、`exec`、`resume`，独立 `COOMI_HOME` | `codex-rs/coomi-cli` |
| Provider 边界 | OpenAI Compatible、OpenAI Responses、Anthropic Messages、Gemini Native；直接使用 Codex canonical types，fail closed | `codex-rs/coomi-provider-adapters` |
| Provider 配置与传输 | 加载 `providers.json`、secret 脱敏、协议鉴权、HTTP/SSE canonical transport | `config.rs`、`http_transport.rs` |
| Embedded Provider Gateway | 普通 `coomi exec`、TUI、resume 经进程内 Gateway 使用 canonical Responses 接入第三方 wire adapter；Provider 差异未进入 Core/ToolRouter/TUI | `gateway.rs`、`coomi-cli/src/main.rs` |
| Capability reducer | Namespace 稳定扁平化并可逆恢复；ToolSearch 降级为同名 function；Freeform/Custom 降级为 `{input:string}`；第三方 provider 不继承 OpenAI hosted tools | `common.rs`、`http_transport.rs`、`model-provider/src/provider.rs` |
| 工具协议安全边界 | 未声明工具、畸形 JSON、非法 ToolSearch/Freeform wrapper 均 fail closed；普通及 Namespace function 均在审批/执行前做客户端 JSON Schema 校验 | `core/src/tools/registry.rs`、`tools/src/json_schema.rs` |
| Schema 引用安全 | root schema 单次 JSON 化，JSON Pointer 解析 `$ref`，验证深度上限 128，循环引用 fail closed | `tools/src/json_schema.rs` |
| Reasoning canonical 生命周期 | OpenAI Compatible 的 `reasoning_content` 映射为 Reasoning Added → Delta → Done，再进入 Message/Tool；非法晚到 reasoning fail closed | `openai_compatible.rs`、`gateway_turn.rs` |
| Provider 探测命令 | `coomi provider test [--full] [--json]` | `codex-rs/coomi-cli` |
| Core mock Turn | 无工具 Turn 一次模型请求完成 | `coomi-integration-tests/tests/responses_turn.rs` |
| C08 幂等 | 断流重试后相同 `call_id` 的 shell 副作用只执行一次 | `coomi-integration-tests/tests/responses_turn.rs` |
| Core Gateway 集成 | OpenAI Compatible 工具循环、非法参数零副作用、reasoning→正文完整回合均通过 | `coomi-integration-tests/tests/gateway_turn.rs` |
| P2 事件不变量 | Gateway 下 Turn/Item 的 thread ID、turn ID、item ID 一致，事件顺序为 Started → Delta → Completed → TurnComplete | `gateway_preserves_turn_and_item_event_identity` |
| P2 同 Turn 控制 | steer 在同一 active Turn 的后续请求中携带新输入；interrupt 在等待 Provider 时立即产生 `TurnAborted(Interrupted)` | `gateway_steer_input_continues_the_active_turn`、`gateway_interrupt_aborts_a_waiting_provider_turn` |
| P2 持久化历史 | rollback 裁掉目标 Turn；冷 resume 保持原 Thread ID；fork 生成新 Thread ID，三者均保持正确 canonical history | `gateway_history_survives_rollback_resume_and_fork` |
| 目标测试 | CLI 4/4、Gateway/Responses 集成 9/9、adapter unit 6/6、协议 8/8；`codex-tools` 89/89、`codex-model-provider` 56/56 | 目标包 `cargo test` |
| 严格静态检查 | 所有新 target 零警告 | `cargo clippy ... --all-targets -- -D warnings` |
| CLI 命令面 | `coomi 1.4.0`，root/exec/resume help 通过且无 Codex 品牌文案 | `target/debug/coomi.exe` |

## 真实 Provider 探测

配置：`~/.coomi/config/providers.json` 的 `opencode-go/deepseek-v4-flash`，协议为 OpenAI Compatible。API key 未写入仓库、日志或报告。

| 探测 | 状态 |
| --- | --- |
| C01 单工具、C02 流式参数、C03 工具续接 | 通过 |
| C04 并行调用、C05 schema 边界 | 通过 |
| C06 服务端 strict | 失败，模型接受非法 enum；必须客户端校验 |
| C07 reasoning/tool 交错 | 通过 |
| C08 断流工具幂等 | mock 集成通过，真实 endpoint 未主动断流 |
| C09 usage/end-turn | 通过 |
| C10 错误响应 | 通过；超长 schema/工具数量上限待补 |
| 真实 `coomi exec` | 通过；精确输出 `COOMI_GATEWAY_OK`，正常退出，无 panic、无残留进程 |

完整探测累计 usage：input 2,857、cached input 1,536、output 582、total 3,439。

2026-07-30 的低成本真实 CLI 回合 usage：input 11,136、cached input 11,008、output 29（其中 reasoning 21）。本次实际未缓存输入为 128 token，但名义上下文仍约 11K；同时 `deepseek-v4-flash` 尚无本地 model metadata，当前使用 fallback metadata。这两项属于 P3/P4 的模型目录与上下文成本优化输入，不能据此宣称成本目标已经达成。

## P0 未关闭项

- 使用同一 endpoint/model 对 Python Coomi、固定 Codex 和 Rust Coomi 跑 B01-B10 动态成本基线；
- 记录 P50/P95 输入、缓存、输出、工具调用、延迟和任务完成率；
- 用真实模型账单运行需要单独费用授权，未授权前只建立 harness，不发起计费请求；
- 将基准结果回填 ADR-006，并校准循环、上下文和工具结果的默认预算。

## P1 未关闭项

- 建立 Windows/Linux CI；
- 裁剪确认无关的 workspace member 和 feature；
- 将 TUI 内部品牌、更新提示和深层运行时文案从 Codex 兼容层逐步迁移为 Coomi；
- 补齐 C10 超长 schema、工具数量上限以及探测结果缓存/TTL；

## P2-P8 当前边界

- 当前完成的是 Embedded Gateway、canonical Provider/Tool 边界和真实 Agent Loop 首个里程碑，不是 P0-P8 全部完成；
- P2 已验证 Gateway 路径下的事件身份/顺序、steer、interrupt、rollout rollback、冷 resume 和 fork；仍需补非正常崩溃后的已完成 Item 恢复、SQLite 显式断言和稳定事件 snapshot；
- P3 仍需 Tool Spec Plan、工具/结果预算、重复调用治理、完整权限/沙箱矩阵及多 provider 实测；
- P4-P8 的上下文预算、compact/memory、Skill/MCP、Coomi TUI 管理中心、迁移发布和稳定化仍待实施。

## 阶段边界

当前 Python 实现保留为只读行为和成本基线。它还没有退出产品路径，不能宣布“零 Python 发布”完成。Codex Core、TUI、权限和沙箱目前仍是完整固定基线，裁剪必须由依赖图、测试和成本数据驱动。
